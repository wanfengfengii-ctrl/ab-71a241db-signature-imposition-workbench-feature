server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # 静态前端
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 同源 /api/* 反代到 API 服务，去掉 /api 前缀
    location /api/ {
        proxy_pass http://api:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
